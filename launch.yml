---
    processes:
    - type: monit
      command: "monit -vv -d 120 -c monitrc -l /home/vcap/logs/monit.log"
      limits:
        memory: 10
      platforms:
        cloudfoundry:
          sidecar_for: ["web"]